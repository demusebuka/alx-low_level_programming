#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 * Return: Always 0 (sucess)
 */
 int main(void)

{
char str[]="\"Programming is like building a multilingual puzzle";

puts(str);

return (0);
}
